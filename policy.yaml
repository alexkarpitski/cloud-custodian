policies:
- name: gcp-instance-invalid-network-delete
  resource: gcp.instance
  mode:
    type: gcp-audit
    methods:
    - beta.compute.instances.insert
  filters:
  - type: invalid-network
    bucket-name: custodian-org-project.appspot.com
    config-path: test/envs1.json
    valid-network-jmespath: vpcx.region_subnet
  actions:
    - delete
